# verify.js (967 satır) analiz notları

## Yapı
- Satır 1-18: require'lar (semantic-score, claim-decomposition, reasoning-trace, contradiction-rules, fuzzy-normalization, semantic-signals, type-lattice, text-utils, entity-resolution)
- Satır 21-262: SAF STANALONE FONKSİYONLAR (~242 satır):
  - normalizeWorkspaceId (21), edgeClaim (26), normalizeForVerify (36), foldTurkishAscii (43), stripCopulaSuffix (53), phraseMatches (61), sharedPrefixLength (72), hasSharedSemanticAnchor (79), isPreventRelation (94), uniqueFlags (99), maxSignalScore (103), buildVerifySemanticTrust (111-262, ~150 satır büyük fonksiyon!)
- Satır 263: `class VerifyService {` — geri kalanı sınıf (263-966)
- Satır 966: `module.exports = VerifyService;` (tek export, class)

## Sınıf yöntemleri
- constructor (264), verify (268-567, ~300 satır - EN BÜYÜK), _verifyResult (568-649), _resolveCanonicalSubjectLookup (650-697), detectContradictions (698-832, ~135), _parseNumericComparison (833-879), _extractSubjectAndPredicate (880-919), _contradictionEvidence (920-933), _extractNumbers (934-951), _getTextCore (952-966)

## Kullanım
- Tek dışarıdan tüketici: kernel.js:7 `const VerifyService = require('./lib/verify');` + satır 135 `this._verifyService = new VerifyService(this);`
- Testler: verify-*.test.js (Kernel üzerinden integration + VerifyService construction via stub kernel)
- Sınıf dışı standalone fonksiyonlar SADECE sınıf içinden çağrılıyor; dışa export edilmemiş.

## Ekstraksiyon planı
Önceki pattern (action-risk-classifier + kernel-v2-native):
1. Satır 1-262'yi `lib/verify-native.js`'e taşı (saf fonksiyonlar; module.exports ile dışa aç)
2. lib/verify.js başına: const native = require('./verify-native'); (veya destructuring)
3. Sınıf içi çağrılar birebir aynı (aynı dosya scope'u olduğu için fonksiyon adları referansları bozulmaz — taşıma sonrası require'la erişim gerekecek: normalizeForVerify → native.normalizeForVerify vb.)
4. Beklenen etki: verify.js 967 → ~705 (262 satır taşınır) → ratchet'ten düşer (<800). 12 dosyalı baseline'dan çıkar.

## Dikkat
- buildVerifySemanticTrust 111-262 ~150 satır; DEFAULT_SEMANTIC_THRESHOLDS semantic-score'dan geliyor.
- phraseMatches/sabitler iç fonksiyonlar arası çağrıyor (normal).
- module.exports tek class olduğu için testler değişmez (class hala aynı dosyada).
- npm pack files listesine lib/verify-native.js eklemeyi unutma.
- ratchet: scripts/file-size-baseline.json --update; "threshold'a ulaşan entry silinmeli" kuralı yok çünkü verify.js 705 < 800 olur.
- Commit mesajı: refactor(arch-3) prefix'i + (#328, V3) gibi diğer PR'lardaki formata uy.

## Önceki PR'lar referans
- #914 cli-helpers (836→715), #915 risk-classify (902→6 shim), #916 docs (kernel-split-plan), #917 kernel.v2 split (975→815), #918 K2 delegation (1543→1461). Hepsi merge.
- Kural: tek amaç tek PR; CI yeşil olunca otonom merge; docs-first; fail-closed bozulmaz.
- Branch adı pattern: refactor/{adı}. Push: git credential store gh ile ayarlı (gh auth token proxy git'te çalışıyor; .user_env source et).
