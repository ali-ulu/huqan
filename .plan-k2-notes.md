# K2 Analiz Notları (context kompaksiyonu öncesi)

## Görev
`kernel.js` _commitBackgroundEdge (satır 555-637, 82 satır) + `_backgroundProvenance` wrapper (534-537) → `lib/background-provenance.js`'e delege et.

## Mevcut yapı
- `kernel.js` satır 9: zaten `const { buildBackgroundProvenance, provenanceFieldsFrom } = require('./lib/background-provenance');`
- `lib/background-provenance.js` (165 satır): buildBackgroundProvenance, provenanceFieldsFrom, failClosedProvenance, admissionRiskFromConfidence export ediyor.
- `_commitBackgroundEdge(from, to, relation, source, opts)` kullanımı:
  - satır 293 (proposeEdge)
  - satır 909 (_crossLink)
  - satır 1124 (_autoThinkTick)
  - satır 1231 (dream)
  - satır 1451 (selfEvolve)

## _commitBackgroundEdge bağımlılıkları (this üzerinden)
1. `normalizeWorkspaceId(opts.workspaceId || 'default')` — utility import var (satır 38)
2. `this._backgroundProvenance(source, ws, extra)` → `buildBackgroundProvenance(source, ws, extra, {contractVersion, trustPolicyPath})` — bu.wrapper, doğrudan buildBackgroundProvenance'ı sarıyor
3. `this._evaluateLearnAdmission(text, admissionOpts, provenance, workspaceId)` — sınıf yöntemi
4. `this._appendAuditEvent(event, provenance, workspaceId)` — sınıf yöntemi
5. `this._admissionReceiptDetails(admission)` — sınıf yöntemi
6. `this.graph.addEdge(from, to, relation, edgeOptions)` — graph

## Tasarım: dependency injection ile saf fonksiyon
`lib/background-provenance.js`'e yeni export:
```js
function commitBackgroundEdge(deps) {
  // deps: {
  //   buildProvenance: (source, ws, extra) => provenance,   (default: buildBackgroundProvenance)
  //   evaluateLearnAdmission: (text, opts, provenance, ws) => admission|null,
  //   appendAuditEvent: (event, provenance, ws) => audit,
  //   admissionReceiptDetails: (admission) => object,
  //   addEdge: (from, to, rel, opts) => edge,
  //   contractVersion, trustPolicyPath,   (provenance opts)
  // }
  return function commitBackgroundEdge(from, to, relation, source, opts = {}) { ... }
}
```
Kernel.js'te:
- Satır 9 require'ına commitBackgroundEdge ekle
- `this._commitBackgroundEdge` yöntemi = injected commitBackgroundEdge({buildProvenance, evaluateLearnAdmission, ...}) çağrısı
- `_backgroundProvenance` wrapper'ı sil (gereksiz kalır)

## Beklenen sonuç
- kernel.js 1543 → ~1445 (-98: 82 _commitBackgroundEdge + wrapper + JSDoc)
- hâlâ 800+ → baseline'da kalır ama küçülür
- background-provenance.js 165 → ~270 (yine 800 altında)

## Test kanıtı gereken dosyalar
- kernel.test.js (var mı kontrol et: ls test/*.js | grep kernel)
- npm run check:file-size -- --update
- package.json files listesine yeni modül gerekmiyor (background-provenance.js zaten listede)

## Önceki PR'lar (bağlam)
- PR #914 (cli refaktor) MERGED, PR #915 (risk extract) MERGED, PR #916 (docs split plan) MERGED, PR #917 (kernel-v2 split) MERGED
- Branch naming: refactor/k2-background-edge-delegation
- gh PR oluştururken backtick'leri escape et (bash hatası vermişti)
- Credential: `git config --global credential.helper '!f(){ echo "username=x-access-token"; echo "password=$(gh auth token)"; }; f'`
- Ratchet: scripts/file-size-baseline.json 12 dosya, 800 threshold


## K2 İlerleme Durumu (güncel)
- PR #918 açıldı: https://github.com/ali-ulu/huqan/pull/918
- Branch: refactor/k2-background-edge-delegation
- Yapılanlar: kernel.js 1543→1460 (delegasyon + orfan JSDoc silindi + _backgroundProvenance wrapper silindi); lib/background-provenance.js 165→286 (commitBackgroundEdge(deps) eklendi, normalizeWorkspaceId import'u eklendi); ratchet --update yapıldı (12 dosya).
- commit/push edildi, PR body yazıldı.
- CI: Node 22 testleri 2 fail:
  1. test/audit-evidence-position.test.js:136 — postMutation 3 bekliyor 2, total 8 bekliyor 6. DÜZELTİLDİ (pin'ler 2/6/4'e çekildi, K2 yorumu eklendi).
  2. test/audit-family-independence.test.js:80 — callSites 8 bekliyor 6. DÜZELTİLECEK: aynı dosyada "claim 1: the kernel chokepoint governs eight call sites" testinde 8→6 yap.
- Bu pin'li testler yapı testi, delegasyonun doğal sonucu — K2 yorumuyla belgelenmeli.
- Kalan: audit-family-independence düzelt + commit/push + CI re-run (gh pr checks otomatik) + yeşilse merge + remote branch sil + kullanıcıya rapor.
- Önceki pre-existing fail: kernel-mutation-admission.test.js test 8 (ANSI renkli git grep, main'de de aynı).
- PR merge kuralı: CI yeşil → squash merge.
- Sonraki adım: kullanıcıya rapor et.


## CI Round 2 durumu (push 7b2cfe0 sonrası)
İki eski pin test düzeltildi ve lokal 20/20 geçiyor. Kalan 3 fail tamamen evidence-propagation ve caller-map ledger testleri:
1. test/evidence-aware-caller-map.test.js:128 — `lib/background-provenance.js: not in the ledger, but calls addEdge, appendAuditEvent` — caller map ledger testine yeni dosya eklenmeli.
2. test/evidence-propagation-audit.test.js:44 — 9 bekliyor 7 (kernel audit sink çağrı sayısı; 2'si modüle gitti).
3. test/evidence-propagation-audit.test.js:68 — 7 bekliyor 4 (bind result sites; 3'ü delegasyon).
Hepsi K2 delegasyonunun doğal sonucu — bu testler repo'daki mutation site sayısını pin ediyor. Düzeltme yaklaşımı: caller map testine background-provenance.js'i ledger'a ekle; evidence-propagation-audit.test.js'te kernel sayılarını 9→7 ve 7→4'e çek + delegasyonu belgeleyen yorum.
Dosya konumları: /home/ubuntu/huqan/test/evidence-aware-caller-map.test.js ve /home/ubuntu/huqan/test/evidence-propagation-audit.test.js.


## evidence-aware-caller-map.test.js:128 düzeltmesi
Test: `assert.deepEqual(new Set(enclosing('kernel.js', [796, 912])), new Set(['learn', '_crossLink']))`.
- Satır 796 hâlâ `_crossLink` içinde (değişmedi).
- Satır 912 artık `alternatives` fonksiyonunda (önceden `learn` idi — delegasyonla satırlar kaydığından değil, learn'ın discard sitesi learn fonksiyonunun 694. satırında: `this._appendAuditEvent(...REJECT...)`).
- Eski learn discard sitesi 912 civarındaydı (delegasyon öncesi); şimdi 694.
- Düzeltme: [796, 912] → [796, 694].
Not: 796, _crossLink içindeki `if (existing)` öncesi addEdge çağrısı — K2 ile _commitBackgroundEdge artık delegate; ama test satır 796'yı hâlâ _crossLink'e kapatıyor, sorun yok.


## CI Round 3 — 4 kalan fail (commit 6aaf3cb)
Lokal 14/14 geçiyor ama CI'da 4462 pass / 3 fail (toplam fail listesi 4 test):
1. test/ingest-source-type-trust-weights.test.js:98 — "ingest source types reach the trust policy"
2. test/ingest-source-type-trust-weights.test.js:177-195 — "the plugin and background edge path reaches the trust policy"
3. test/mutation-admission-boundary.contract.test.js:217 — "mutation admission: no production file gains a new direct sink call" (addEdge, appendAuditEvent çağrısı background-provenance.js'te — ledger'e ekle)
4. test/pre-site-refusal-survives-audit-failure.test.js:118/129 — expected 9 actual 7 (kernel audit call site pin'i — aynı pattern, 9→7)
NOT: Run 32186482801 hangi commit'i test ediyor? Push 6aaf3cb sonrası başladı — ama log 21:13-21:14 (push 6aaf3cb sonrası). Run muhtemelen önceki push'u test ediyor (queue). Bir sonraki run otomatik tetiklenecek. AMA ilk 2 fail ingest-source-type testleri — bunlar background-provenance.js'in addEdge/appendAuditEvent ledger'ı ile ilgili (fail 3 aynı konudan). Hepsini fix'leyip push'la.

## Gerekli düzeltmeler
A. pre-site-refusal-survives-audit-failure.test.js:118 — 9→7 pin (9 call site pre-delegation, 7 post; kernel'de 6 call + 1 definition pattern muhtemelen). Satır 118-130'u oku.
B. mutation-admission-boundary.contract.test.js:217 — "no production file gains a new direct sink call": background-provenance.js addEdge/appendAuditEvent çağırıyor → bu test production mutation dosyası ledger'ına background-provenance.js eklenmeli (veya test, background-provenance'ın zaten ledger'da olduğunu kabul ediyor). Satır 200-230'u oku.
C. ingest-source-type-trust-weights.test.js:98 ve 177 — "reach the trust policy" — background-provenance.js trust policy'yi kullanıyor (applyTrustPolicyToProvenance) — bu test muhtemelen source type ağırlıklarını kontrol ediyor; background proventance source type'ı yeni. Satır 90-105 ve 170-200'ü oku.


## K2 GÜNCEL DURUM (son commit öncesi)
Branch: refactor/k2-background-edge-delegation, PR #918 (https://github.com/ali-ulu/huqan/pull/918). Kernel.js 1543→1460 satır, lib/background-provenance.js 165→286 (commitBackgroundEdge deps eklendi). package.json files listesine eklendi, ratchet güncellendi.

Tüm test düzeltmeleri LOKAL tamamdır (7 dosya 55/56 → son düzeltmeyle 56/56 olması bekleniyor):
1. test/audit-evidence-position.test.js — postMutation 3→2, total 8→6, pre 5→4 ✓
2. test/audit-family-independence.test.js — 8→6 ✓
3. test/evidence-aware-caller-map.test.js — satır [796,912]→[796,694] ✓
4. test/evidence-propagation-audit.test.js — direct 9→7, writes 21→19, bound 7→4, propagation floor 4→3, başlık güncellendi ✓
5. test/pre-site-refusal-survives-audit-failure.test.js — 17→15, kernel 9→7 ✓
6. test/ingest-source-type-trust-weights.test.js:177 — kernel._backgroundProvenance → lib'deki buildBackgroundProvenance ✓
7. test/mutation-admission-boundary.contract.test.js — ledger'a 'lib/background-provenance.js': { addEdge:1, appendAuditEvent:1 } eklendi (scan regex dotsuz alias'ları saymıyor), unrouted 22, routed 24→26, total 46→48 ✓

KALAN ADIMLAR:
1. Lokal son doğrulama (yukarıdaki 7 dosya).
2. git add/commit/push (tüm test dosyaları).
3. gh pr checks 918 — yeşilse gh pr merge 918 --repo ali-ulu/huqan (squash).
4. Remote branch sil: gh branch delete refactor/k2-background-edge-delegation.
5. Kullanıcıya rapor: kernel.js 1543→1460; ratchet'te hâlâ kernel.js, kernel.v2.js (815), memory-store.js vb. K2 sonrası doğal sıradaki işler: kernel.v2 V2-B contradiction seam (fonksiyonel PR'da), #279/#292 Cloudflare kararı kullanıcıda.
Not: Önceki PR'lar #914, #915, #916, #917 merge edildi. Ratchet 12 dosyada.
