#!/bin/sh
# HUQAN pre-commit hook: fast checks only.
# Install:
#   cp .github/hooks/pre-commit .git/hooks/pre-commit
#   chmod +x .git/hooks/pre-commit
set -eu
npm run lint
npm run check:cycles
npm run check:module-boundary
