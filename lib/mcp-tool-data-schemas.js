'use strict';

// Output data schemas for every MCP tool, grouped by domain in
// mcp-tool-data-schemas-*.js (#2172); this module adds the envelope
// schemas and keeps the one export surface callers use.

const { buildEnvelopeSchema } = require('./mcp-envelope-schema');
const { AGENT_CONTINUATION_SCHEMA, AGENT_PLAN_SCHEMA, AGENT_RUN_SCHEMA, AGENT_STEP_SCHEMA } = require('./mcp-tool-data-schemas-agent');
const { ADVOCATE_DATA_SCHEMA, APPROVAL_DECISION_DATA_SCHEMA, COMPLIANCE_AUDIT_SCHEMA, SYSTEM_STATUS_SCHEMA, TOOL_APPROVAL_SCHEMA, TOOL_POLICY_SCHEMA } = require('./mcp-tool-data-schemas-governance');
const { INGEST_EXECUTE_DATA_SCHEMA, INGEST_PREVIEW_DATA_SCHEMA, INGEST_RUN_DATA_SCHEMA } = require('./mcp-tool-data-schemas-ingest');
const { ASK_DATA_SCHEMA, COMPARE_DATA_SCHEMA, DREAM_DATA_SCHEMA, FRACTAL_LEARN_DATA_SCHEMA, LEARN_DATA_SCHEMA, REASON_DATA_SCHEMA, SELF_EVOLVE_DATA_SCHEMA } = require('./mcp-tool-data-schemas-knowledge');
const { MEMORY_SEARCH_DATA_SCHEMA, TRUST_RECEIPT_DATA_SCHEMA, VERIFY_DATA_SCHEMA } = require('./mcp-tool-data-schemas-trust');

const ENVELOPE_OUTPUT_SCHEMA = buildEnvelopeSchema({ type: 'object' });
const VERIFY_ENVELOPE_OUTPUT_SCHEMA = buildEnvelopeSchema(VERIFY_DATA_SCHEMA);

module.exports = {
  LEARN_DATA_SCHEMA,
  ASK_DATA_SCHEMA,
  REASON_DATA_SCHEMA,
  COMPARE_DATA_SCHEMA,
  DREAM_DATA_SCHEMA,
  FRACTAL_LEARN_DATA_SCHEMA,
  SELF_EVOLVE_DATA_SCHEMA,
  AGENT_STEP_SCHEMA,
  AGENT_PLAN_SCHEMA,
  AGENT_RUN_SCHEMA,
  AGENT_CONTINUATION_SCHEMA,
  TOOL_POLICY_SCHEMA,
  TOOL_APPROVAL_SCHEMA,
  APPROVAL_DECISION_DATA_SCHEMA,
  ADVOCATE_DATA_SCHEMA,
  MEMORY_SEARCH_DATA_SCHEMA,
  TRUST_RECEIPT_DATA_SCHEMA,
  SYSTEM_STATUS_SCHEMA,
  COMPLIANCE_AUDIT_SCHEMA,
  VERIFY_DATA_SCHEMA,
  INGEST_PREVIEW_DATA_SCHEMA,
  INGEST_EXECUTE_DATA_SCHEMA,
  INGEST_RUN_DATA_SCHEMA,
  VERIFY_ENVELOPE_OUTPUT_SCHEMA,
};
