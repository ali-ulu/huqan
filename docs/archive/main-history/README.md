# Main History Archive

This directory preserves selected historical artifacts from the old `main` branch.

Source branch:

- `origin/main @ 3ce146589e5618217a88c7c0147e13e2fa437c0c`

Current canonical base:

- `claude/practical-knuth-0ecsze @ 9d296c7be3873be91d2bbafdad57fdc244a0be0c`

These files are preserved for history and evidence only. They are not active runtime, test, demo, product, or release surfaces.

Archived tests under `docs/archive/main-history/tests/` are not part of the current canonical test suite. Archived historical test files are stored as `.txt` so they are not discovered by the active canonical test runner. Active adoption requires a separate scoped PR with compatibility validation.

No source history rewrite was performed. This archive does not merge `main` into canonical, does not overwrite `main`, does not change the default branch, and does not delete branches.
