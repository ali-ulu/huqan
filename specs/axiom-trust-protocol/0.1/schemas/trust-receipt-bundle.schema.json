{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://huqan.dev/specs/axiom-trust-protocol/0.1/schemas/trust-receipt-bundle.schema.json",
  "title": "ATP v0.1 Trust Receipt Bundle",
  "description": "Self-contained, hash-sealed export of a chained trust receipt sequence. Structural shape only: hash and chain correctness are defined by RECEIPT-BUNDLE.md and cannot be expressed in JSON Schema.",
  "type": "object",
  "required": [
    "sealVersion",
    "schemaVersion",
    "workspaceId",
    "exportedAt",
    "receiptCount",
    "bundleHash",
    "receipts"
  ],
  "additionalProperties": false,
  "properties": {
    "sealVersion": {
      "type": "string",
      "enum": ["huqan-bundle-seal-v2"],
      "description": "Names the canonical bundleHash input. Absent on legacy bundles, whose seal covered receipts only and whose envelope is therefore unauthenticated."
    },
    "schemaVersion": {
      "type": "string",
      "enum": ["v4-receipt-bundle-v1", "v4-receipt-bundle-v2"],
      "description": "v2 when any receipt declares v4-receipt-v2, otherwise v1."
    },
    "workspaceId": {
      "type": "string",
      "minLength": 1
    },
    "exportedAt": {
      "type": "string",
      "minLength": 1,
      "description": "ISO-8601 export timestamp. Covered by bundleHash under sealVersion huqan-bundle-seal-v2."
    },
    "receiptCount": {
      "type": "integer",
      "minimum": 0,
      "description": "Entry count. Covered by bundleHash, and a conforming verifier additionally rejects a bundle whose receiptCount disagrees with receipts.length."
    },
    "bundleHash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Lowercase hex SHA-256 over canonicalJson of the canonical seal payload: {sealVersion, schemaVersion, workspaceId, exportedAt, receiptCount, receipts}."
    },
    "receipts": {
      "type": "array",
      "items": { "$ref": "#/definitions/chainedReceipt" }
    },
    "bundleSignature": {
      "type": "object",
      "required": ["schemaVersion", "algorithm", "keyReference", "signature"],
      "additionalProperties": false,
      "properties": {
        "schemaVersion": { "type": "string", "enum": ["huqan.receipt-bundle-signature.v1"] },
        "algorithm": { "type": "string", "enum": ["ed25519"] },
        "keyReference": { "type": "string", "minLength": 1 },
        "signature": { "type": "string", "minLength": 1 }
      }
    }
  },
  "definitions": {
    "chainedReceipt": {
      "type": "object",
      "required": [
        "schemaVersion",
        "receiptId",
        "receiptKind",
        "decision",
        "verdict",
        "status",
        "admissionId",
        "workspaceId",
        "actor",
        "agentId",
        "memoryDraftId",
        "provenanceId",
        "trustPolicyVersion",
        "approvalId",
        "approvalStatus",
        "reason",
        "riskScore",
        "createdAt",
        "metadata",
        "previousReceiptHash",
        "receiptHash"
      ],
      "additionalProperties": false,
      "properties": {
        "schemaVersion": {
          "type": "string",
          "enum": ["v4-receipt-v1", "v4-receipt-v2"]
        },
        "receiptId": { "type": "string", "minLength": 1 },
        "receiptKind": { "type": "string", "minLength": 1 },
        "decision": { "type": "string", "minLength": 1 },
        "verdict": { "type": "string", "minLength": 1 },
        "status": { "type": "string", "minLength": 1 },
        "admissionId": { "type": "string", "minLength": 1 },
        "workspaceId": { "type": "string", "minLength": 1 },
        "actor": { "type": "string" },
        "agentId": { "type": "string" },
        "memoryDraftId": { "type": "string" },
        "provenanceId": { "type": "string", "minLength": 1 },
        "trustPolicyVersion": { "type": "string", "minLength": 1 },
        "approvalId": { "type": "string" },
        "approvalStatus": { "type": "string" },
        "reason": { "type": "string" },
        "riskScore": { "type": "number" },
        "createdAt": { "type": "string", "minLength": 1 },
        "metadata": { "type": "object" },
        "trustRoot": {
          "type": "object",
          "description": "Present only on v4-receipt-v2 records."
        },
        "previousReceiptHash": {
          "type": "string",
          "minLength": 1,
          "description": "Predecessor receiptHash, or the exact string genesis:v4-receipt-chain for the first record."
        },
        "receiptHash": {
          "type": "string",
          "pattern": "^[0-9a-f]{64}$"
        }
      }
    }
  }
}
